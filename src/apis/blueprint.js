const getList = {
    url: '/blueprint-api/blueprints/list', method: 'GET'
};

const getDetail = {};

const doDelete = {};

const doEdit = {};

const doAdd = {};

const apis = {doAdd, doEdit, doDelete, getDetail, getList};

export default apis;
